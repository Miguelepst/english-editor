# Archivo de prueba para evidenciar pre-commit
import os  # ❌ Imports mal ordenados (ruff I001)
import sys


def suma(x, y):  # ❌ Formato inconsistente (black)
    return x + y  # ❌ Sin espacios (black)
